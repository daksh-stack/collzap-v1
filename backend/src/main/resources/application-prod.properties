# Active only under SPRING_PROFILES_ACTIVE=prod (see docker-compose.yml).
# Debug SQL logging is a dev convenience — formatting and logging every query
# adds real CPU/I/O overhead per request under production load, for no benefit
# once queries are already correct.
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
